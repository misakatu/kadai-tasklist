<h1>タスク一覧</h1>

<ol>
  <% @tasks.each do |task| %>
    <li><%= link_to task.status, task %> &gt; <%= link_to task.content, task %></li>
  <% end %>
</ol>

<%= paginate @tasks %>

<%= link_to "タスクを新規作成する", new_task_path %>